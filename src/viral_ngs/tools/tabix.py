#	'tabix':	'/idi/sabeti-data/software/tabix/tabix-0.2.6',

# (actually, we might not really need this tool if we rely on pysam?)
# use instead: util.vcf.vcf_bgzip_index('inputfile.vcf', 'outputfile.vcf.gz')
# and util.vcf.TabixReader and util.vcf.VcfReader
