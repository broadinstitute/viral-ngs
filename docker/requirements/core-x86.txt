# x86-only bioinformatics tools
# These packages are only available on x86_64 architecture (no ARM64 builds in bioconda)
# They are installed separately from the main core requirements
# Install with: install-conda-deps.sh --x86-only requirements/core-x86.txt

# Aligners (bioconda, x86 only)
mvicuna=1.0
novoalign=3.09.04

# CD-HIT auxiliary tools (includes cd-hit-dup for duplicate removal)
cd-hit-auxtools>=4.6.8
