# Conda dependencies for viral-ngs core
#
# All Python runtime dependencies are installed via conda for faster
# installation and better binary compatibility. This includes packages
# with C extensions that would otherwise require compilation.
#
# Note: General utilities (parallel, pigz, jq, etc.) are in baseimage.txt
# Note: x86-only packages (novoalign, cd-hit-auxtools) are in core-x86.txt

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0

# Python runtime dependencies
arrow>=0.12.1
biopython>=1.72
jinja2>=2.11.3
lz4>=2.2.1
matplotlib>=3.9.0
pandas>=2.0.0
pillow>=12.3.0
psutil>=6.1.0
pybedtools>=0.7.10
pysam>=0.23.0
pyyaml>=6.0
scipy>=1.10.0
seaborn>=0.12.0
zstandard>=0.23.0

# Bioinformatics tools
bbmap>=39.10
bcftools>=1.10
bedtools>=2.31.1
bwa>=0.7.19
cd-hit>=4.8.1
fastqc>=0.12.1
fgbio>=3.1.2
illumina-interop=1.5.0
lbzip2>=2.5
lz4-c>=1.9.4
minimap2>=2.30
picard>=3.4.0
prinseq>=0.20.4
sambamba>=1.0.1
samtools>=1.21
splitcode>=0.31.6
trimmomatic>=0.40
