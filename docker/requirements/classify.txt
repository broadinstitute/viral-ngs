# Metagenomic classification tools
# Note: x86-only packages (bmtagger, kallisto, kb-python) are in classify-x86.txt
anndata>=0.10
blast>=2.15.0
# Cap below 2.0 so a future major-version release does not silently
# change SQL behavior in the VirNucPro read-by-contig classifier
# (which uses EXCLUDE, window functions, and read_csv with the
# explicit columns= arg). Revisit the pin when DuckDB 2.0 ships and
# the SQL paths have been validated against it.
duckdb>=1.1.0,<2.0
genomad>=1.11.0
# Pin <2.19: conda-forge tensorflow 2.19.1 ships libtensorflow_cc.so.2
# with PT_GNU_STACK marked executable, which glibc refuses to load on
# hardened kernels (ImportError: "cannot enable executable stack as
# shared object requires"). Affects any genomad import on the classify
# image. Revisit when the conda-forge packaging bug is fixed upstream.
tensorflow<2.19
kma>=1.4.0
kmc>=3.2.1
kraken2>=2.1.3
krona>=2.8.1
last>=1541
centrifuger>=1.0
